<div class="blog-sidebar-item">
  <h3><%= t '.title' %></h3>

  <ul class="blog-sidebar-taglist">
    <% tags.each do |tag| %>
      <li><%= link_to tag.name, blog_tag_path(tag.id) %></li>
    <% end %>
  </ul>
</div>
